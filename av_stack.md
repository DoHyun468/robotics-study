# 자율주행 인지 스택 — 추정·융합·추적·계획 (수식·유도)

[SLAM 트랙](slam.md)이 "**나는 어디에 있나"**(ego-localization)를 다뤘다면, 이 장은 그 형제 문제들을 다룬다 — **"남들은 어디에 있고 어디로 가나"**(검출·추적), **"센서들을 어떻게 하나로 합치나"**(융합·필터링), **"그래서 나는 어디로 가나**"(계획). LiDAR·카메라 기반 자율주행 스택의 공통 지식층이고, 형식은 [SLAM 수식·유도](slam_math.md)와 동일하다: (1) 무엇을 푸는가 → (2) 수식과 유도 → (3) 기호 정의 → (4) 우리 실측·트랙과의 연결.

**정직성 표기**: 이 장은 문헌·표준 레시피의 개념 정리다(§8의 연결 항목 외에는 직접 구현·실측이 아니다). 수식 유도는 전부 자체 전개이며, 특정 벤치 절대 수치는 싣지 않는다.

## 0. 스택 한 장

```
센서: LiDAR · 카메라 · IMU · (GNSS/휠 오도메트리)
  │  ① 캘리브레이션(공간 정렬) + 동기화(시간 정렬)   ← 모든 융합의 전제 (§2.1)
  ▼
검출(detection): LiDAR 3D 검출(§4) · 카메라 2D/3D 검출
  │  ② 융합(fusion): early / feature / late  (§2.3)
  ▼
추적(tracking): 데이터 연관 + 필터 추정 + 트랙 관리  (§5)
  │  ③ 예측(prediction): 각 객체의 미래 궤적
  ▼
계획(planning): 경로 → 거동 → 궤적  (§6)  → 제어(control)
                                          
자기 위치(ego-pose)는 [SLAM/Localization](slam.md)이 병렬로 공급
```

전 구간을 관통하는 수학은 사실 두 개다 — **최소자승/가우시안 추정**(§1–§2, §5)과 **탐색/최적화**(§6). SLAM 트랙에서 유도한 ICP·pose-graph의 선형화-정규방정식 기법이 여기서도 같은 얼굴로 반복된다.

---

## 1. 상태 추정 — 베이즈 필터에서 칼만 필터까지

### 1.1 무엇을 푸는가

움직이는 대상(자차든 타차든)의 상태 $x_k$(위치·속도·방향각 등)를, 노이즈 낀 측정 $z_{1:k}$로부터 **매 스텝 재귀적으로** 추정한다. 핵심 재귀는 두 박자다:

$$
\underbrace{\text{bel}^-(x_k)=\int p(x_k\mid x_{k-1},u_k)\,\text{bel}(x_{k-1})\,dx_{k-1}}_{\text{예측(predict): 운동 모델로 전파}}
\qquad
\underbrace{\text{bel}(x_k)\propto p(z_k\mid x_k)\,\text{bel}^-(x_k)}_{\text{갱신(update): 베이즈 정리}}
$$

- $\text{bel}(x_k)=p(x_k\mid z_{1:k})$: 지금까지 측정을 다 본 뒤의 믿음(belief).
- 예측은 Chapman–Kolmogorov(전확률), 갱신은 베이즈 정리 — **마르코프 가정**(현재 상태가 과거를 요약)이 이 재귀를 성립시킨다.

이 적분은 일반적으로 못 푼다. **선형 모델 + 가우시안 노이즈**라는 특수한 경우에 닫힌 해가 존재하고, 그것이 칼만 필터다.

### 1.2 모델 정의 (선형-가우시안)

$$
x_k = F\,x_{k-1} + B\,u_k + w_k,\quad w_k\sim\mathcal N(0,Q)
\qquad\qquad
z_k = H\,x_k + v_k,\quad v_k\sim\mathcal N(0,R)
$$

- $F$: 상태 전이(운동 모델), $B u_k$: 제어 입력(트래킹에선 보통 생략), $Q$: 프로세스 노이즈(모델이 못 담는 가감속 등).
- $H$: 관측 행렬(상태에서 측정으로), $R$: 측정 노이즈(센서 사양).

**예측 단계 유도.** 가우시안의 선형변환은 가우시안: $x\sim\mathcal N(\hat x, P)$이면 $Fx+w \sim \mathcal N(F\hat x,\ FPF^\top + Q)$. 따라서

$$
\hat x_k^- = F\hat x_{k-1},\qquad P_k^- = F P_{k-1} F^\top + Q.
$$

(공분산 전파 $FPF^\top$는 $\operatorname{Cov}(Fx)=F\operatorname{Cov}(x)F^\top$에서 — SLAM 트랙의 불확실성 전파와 같은 항등식이다.)

### 1.3 갱신 단계 — 칼만 게인의 유도

측정이 들어오면 **잔차**(innovation)와 그 공분산을 정의한다:

$$
y_k = z_k - H\hat x_k^-,\qquad S_k = H P_k^- H^\top + R.
$$

$S_k$의 의미: 잔차의 흩어짐 = "예측이 흔들리는 만큼($HP^-H^\top$) + 센서가 흔들리는 만큼($R$)". 추정 갱신을 선형 보정 형태로 놓는다:

$$
\hat x_k = \hat x_k^- + K\,y_k.
$$

이때 사후 오차 $e_k = x_k-\hat x_k$의 공분산은 (임의의 $K$에 대해 성립하는 **Joseph form**):

$$
P_k = (I-KH)\,P_k^-\,(I-KH)^\top + K R K^\top.
$$

**최적 $K$ 유도**: 사후 오차의 총분산 $\operatorname{tr}(P_k)$를 $K$로 미분해 0으로 둔다. 행렬 미분 항등식 $\frac{\partial}{\partial K}\operatorname{tr}(KAK^\top)=K(A+A^\top)$, $\frac{\partial}{\partial K}\operatorname{tr}(KA)=A^\top$을 쓰면

$$
\frac{\partial \operatorname{tr}(P_k)}{\partial K}
= -2\,(H P_k^-)^\top + 2\,K\underbrace{(H P_k^- H^\top + R)}_{S_k} = 0
\ \Longrightarrow\
\boxed{\,K = P_k^- H^\top S_k^{-1}\,}
$$

이 $K$를 Joseph form에 대입하면 익숙한 단순형이 나온다:

$$
P_k = (I - K H)\,P_k^-.
$$

**직관 — 게인은 불확실성의 비율이다.** 1차원($H=1$)로 줄이면

$$
K=\frac{\sigma_{\text{pred}}^2}{\sigma_{\text{pred}}^2+\sigma_{\text{meas}}^2}
$$

— 예측이 불확실할수록($P^-\uparrow$) 측정을 크게 믿고, 센서가 나쁠수록($R\uparrow$) 예측을 지킨다. "**칼만 필터 = 불확실성 가중 평균의 재귀판**"이라는 한 줄이 여기서 나온다. 이 관점이 §2의 센서 퓨전으로 그대로 이어진다.

### 1.4 운동 모델 — 트래킹에서 실제 쓰는 형태

**등속(CV, constant velocity)** 모델, 상태 $x=[\,p;\ \dot p\,]$:

$$
F=\begin{bmatrix}I & \Delta t\,I\\ 0 & I\end{bmatrix},
\qquad
H=\begin{bmatrix}I & 0\end{bmatrix}\ (\text{위치만 측정}).
$$

- 검출기는 위치(박스)만 주므로 **속도는 관측되지 않지만**, 두 스텝 이상의 위치 측정이 $F$를 통해 속도를 간접 관측한다 — 필터가 "미분기" 역할을 하는 셈.
- $Q$는 가감속을 흡수한다(백색 가속도 가정의 이산화). $Q$가 너무 작으면 기동하는 차를 놓치고, 너무 크면 노이즈를 따라다닌다 — 실무 튜닝의 핵심 노브.
- 차량엔 방향각·회전율을 넣은 **CTRV**(constant turn rate & velocity)가 흔하다 — 이건 비선형이라 §1.5의 EKF가 필요해진다.

### 1.5 EKF — 비선형의 1차 선형화

운동·관측이 비선형이면($x_k=f(x_{k-1})+w$, $z_k=h(x_k)+v$) 현재 추정점에서 **야코비안으로 1차 근사**한다:

$$
F_k=\left.\frac{\partial f}{\partial x}\right|_{\hat x_{k-1}},\qquad
H_k=\left.\frac{\partial h}{\partial x}\right|_{\hat x_k^-}
$$

나머지 수식은 §1.2–1.3과 동일($F,H$ 자리에 야코비안). 전형적 사례:

- **극좌표 측정**(거리·방위각 센서): $h(x)=\big[\sqrt{p_x^2+p_y^2},\ \operatorname{atan2}(p_y,p_x)\big]^\top$ — 야코비안을 손으로 쓸 수 있어야 하는 대표 예.
- **좌표 변환·각도가 끼는 차량 추적**: yaw가 상태에 들어가는 순간(CTRV) 전이가 삼각함수를 포함 — EKF의 주 무대.

한계와 대안 한 줄씩: 선형화 오차가 큰 강비선형에선 **UKF**(시그마 포인트로 분포 자체를 통과시킴 — 야코비안 불필요), 다봉(multimodal) 분포에선 **파티클 필터**(샘플 기반 — 납치 문제·모호한 연관). SLAM 쪽 대비도 여기서: 필터 기반(EKF-SLAM/MSCKF)은 상태를 요약해 나가고, [그래프 기반](slam_math.md)은 전 궤적을 유지해 재선형화한다 — 트래킹은 객체당 상태가 작아 **필터가 표준**이다.

### 1.6 상보 필터 — 융합의 최소 예제 (IMU 자세)

자이로 적분(고주파 정확, 저주파 드리프트)과 가속도계 중력방향(저주파 정확, 고주파 노이즈)의 결합:

$$
\hat\theta_k=\alpha\,(\hat\theta_{k-1}+\omega\,\Delta t)+(1-\alpha)\,\theta_{\text{acc}},\qquad \alpha\approx0.98
$$

주파수 영역으로 읽으면 자이로에 고역통과, 가속도계에 저역통과를 걸어 합친 것 — "**각 센서를 잘하는 주파수 대역에서만 믿는다**"는 융합 사상의 최소 구현이며, 칼만 필터는 이 $\alpha$를 불확실성으로부터 매 스텝 자동 계산하는 일반화라고 볼 수 있다.

---

## 2. 센서 퓨전 — 정렬, 확률, 결합 수준

### 2.1 융합의 두 전제 — 이게 안 되면 융합이 아니라 오염이다

**공간 정렬(extrinsic).** 모든 측정을 한 좌표계로 가져오는 강체변환 $T\in SE(3)$ — LiDAR↔카메라, 센서↔차체. 우리 트랙에서 직접 푼 [hand-eye AX=XB](perception.md)가 정확히 이 문제의 로봇판이고, 회전축 다양성이 없으면 병진이 관측 불가라는 랭크 구조까지 동일하다.

**시간 정렬(synchronization).** 두 센서가 **다른 시점의 세계**를 말하면, 동적 장면에서 융합 결과에 고스트·편향이 생긴다. 시속 60km 차량은 10ms에 17cm를 움직인다 — LiDAR 한 스캔(수십 ms)의 회전 중에도 세계가 움직이므로 **motion compensation**(스캔 중 ego-motion으로 포인트별 시각 보정)이 표준이다. 시간 오프셋 자체를 추정 대상으로 넣는 접근(각속도 프로파일 정합)은 우리가 cam-IMU에서 직접 수행한 층이다(오프셋 2.1ms, [perception](perception.md)).

### 2.2 확률적 관점 — 융합은 가우시안 곱이다

독립 소스 두 개가 같은 양을 추정했다고 하자: $\mathcal N(\mu_1,\sigma_1^2)$, $\mathcal N(\mu_2,\sigma_2^2)$. 두 가우시안의 곱(정규화 후)은 다시 가우시안이고,

$$
\mu=\frac{\sigma_2^2\,\mu_1+\sigma_1^2\,\mu_2}{\sigma_1^2+\sigma_2^2}
=\frac{\mu_1/\sigma_1^2+\mu_2/\sigma_2^2}{1/\sigma_1^2+1/\sigma_2^2},
\qquad
\frac{1}{\sigma^2}=\frac{1}{\sigma_1^2}+\frac{1}{\sigma_2^2}.
$$

**유도 스케치**: 로그를 취하면 두 이차식의 합 — $-\tfrac{(x-\mu_1)^2}{2\sigma_1^2}-\tfrac{(x-\mu_2)^2}{2\sigma_2^2}$을 $x$에 대해 완전제곱으로 다시 묶으면 위 $\mu,\sigma^2$가 나온다. 세 가지를 읽을 수 있다:

1. **정밀도(precision, $1/\sigma^2$)가 가중치다** — 좋은 센서가 더 크게 반영된다.
2. **정밀도는 더해진다** — 융합하면 어느 단독 센서보다 항상 불확실성이 준다(독립 가정 하).
3. 다변량으로 일반화하면 **정보 행렬(information matrix) $\Lambda=P^{-1}$가 덧셈**으로 융합된다 — [pose-graph](slam_math.md)의 정보 행렬 가중 최소자승과 같은 대수다. §1.3의 칼만 갱신은 이 곱을 "예측 가우시안 × 측정 가우시안"에 적용한 특수형이다.

### 2.3 결합 수준 3층

| 수준 | 무엇을 합치나 | 장점 | 단점 | 카메라+LiDAR 예 |
|---|---|---|---|---|
| **early** (원시) | 원시 데이터끼리 | 정보 손실 최소 | 정밀 시공간 정렬 요구, 한 센서 고장에 전체 취약 | 포인트에 픽셀 색/시맨틱을 투영해 부착(포인트 페인팅 계열) |
| **feature** (중간) | 각 센서의 특징을 공통 공간에서 | 학습이 상호작용을 활용, 실무 표준 | 공통 공간 설계 필요 | 카메라·LiDAR 특징을 **BEV 공간**에 모아 융합하는 계열 |
| **late** (결정) | 각 센서의 출력(검출·트랙) | 모듈러·강건(센서 탈락 대응) | 센서 간 상호작용 정보 소실 | 카메라 검출과 LiDAR 검출을 §5의 연관으로 병합 |

**camera vs LiDAR 상보성**: 카메라 = 의미·질감·색·저가, 거리 추정 약함·조명 민감 / LiDAR = 미터급 직접 기하·조명 불변, 희소·의미 없음·비/눈·저반사(검은 차) 취약. 약점이 정확히 반대라 묶는다. 우리 [depth A/B 실측](perception.md)(mono 7.8mm → stereo 0.4mm)이 "기하 센서를 더할 때 무엇이 좋아지는가"의 소규모 실증이다.

### 2.4 비동기·다주파 융합

센서 주기가 다르면(IMU 수백 Hz, 카메라 수십 Hz, LiDAR ~10Hz) §1의 재귀가 자연스러운 틀이 된다 — **고주파 센서로 예측(전파)을 돌리고, 저주파 센서가 도착할 때만 갱신**한다. 측정이 지연 도착하면(out-of-sequence) 상태 버퍼를 두고 그 시점으로 되돌려 재적용하는 것이 표준 처방. "주파수가 다른 소스의 정렬" 문제는 학습 파이프라인에서도 같은 얼굴로 나타난다([VLA의 이질 주파수 시연 데이터 정렬](vla.md) — 액션 리샘플·chunking).

---

## 3. 좌표계 하나 더 — BEV(Bird's-Eye View)가 표준인 이유

자율주행 인지·계획이 유독 **BEV**(지면 평면 위 2D)로 수렴한 데는 구조적 이유가 있다:

1. **물체가 지면 위에 있다** — z축 겹침이 거의 없어, 3D 문제가 사실상 2D+높이로 분해된다.
2. **거리 불변 스케일** — 원근 왜곡이 있는 이미지 평면과 달리, BEV에선 같은 차가 어디 있든 같은 크기다(컨볼루션의 평행이동 등변성과 궁합).
3. **융합·추적·예측·계획이 같은 좌표를 쓴다** — 카메라 특징을 BEV로 들어올리고, LiDAR를 BEV로 투영하면 §2.3의 feature 융합 공간이 되고, §5의 트래킹과 §6의 계획이 그대로 그 위에서 돈다.

이미지→BEV 변환의 핵심 난제는 **깊이의 모호성**(픽셀 하나가 광선 전체에 대응) — 픽셀별 깊이 분포를 예측해 특징을 광선 위에 뿌리는 lift 계열과, BEV 쿼리가 이미지를 어텐션으로 참조하는 쿼리 계열이 두 표준 답이다. "기하 제약(카메라 모델)을 아키텍처에 주입한다"는 점에서 [vision 챕터](vision/index.md)의 투영 기하가 그대로 재사용되는 지점.

---

## 4. LiDAR 3D 인식 파이프라인

### 4.1 고전 기하 파이프라인 — 지면 제거 → 클러스터링 → 박스 피팅

딥러닝 이전(그리고 지금도 fallback·저연산 경로로) 쓰이는 뼈대:

```
포인트클라우드 → ① 지면 제거 → ② 클러스터링 → ③ 박스 피팅(L-shape) → ④ 분류 → §5 트래킹
```

**① 지면 제거 — RANSAC 평면 피팅.** 평면 모델 $n^\top p + d=0$ ($\|n\|=1$). 무작위 3점으로 평면 가설을 만들고, 인라이어($|n^\top p_i+d|<\tau$) 수가 최대인 가설을 채택. **반복 수 유도**: 인라이어 비율 $w$, 성공 확률 $p$를 원할 때, 한 번의 3점 샘플이 전부 인라이어일 확률은 $w^3$이므로

$$
(1-w^3)^N \le 1-p
\ \Longrightarrow\
N \ge \frac{\log(1-p)}{\log(1-w^3)}.
$$

지면은 보통 포인트의 큰 비율이라($w$ 큼) 적은 반복으로 충분하다. 실무 보강: 경사·연석 대응을 위해 영역별 다중 평면, 또는 링/그리드 기반 높이 필터. (RANSAC 자체는 [매칭 트랙](reviews/matching.md)의 아웃라이어 제거와 동일 도구 — 모델이 F-행렬에서 평면으로 바뀌었을 뿐.)

**② 클러스터링.** 지면을 빼고 남은 점을 물체 후보로 묶는다.

- **유클리드 클러스터링**: 반경 $\varepsilon$ 이웃 그래프의 연결 요소(connected components). 단순·빠름, kd-tree로 이웃 탐색.
- **DBSCAN**: $\varepsilon$-이웃이 $\text{minPts}$ 이상인 점을 core로, core에서 도달 가능한 점들을 한 클러스터로, 어디에도 못 붙는 점을 noise로. 밀도 기준이라 **클러스터 수를 미리 정할 필요가 없고 노이즈를 자연 배제** — k-means와의 결정적 차이. 원거리일수록 포인트가 희소해지므로 $\varepsilon$을 거리에 따라 키우는 적응형이 실무 팁.

**③ L-shape 피팅.** 차량은 스캔에서 보통 두 면(측면+전/후면)만 보인다 — 직사각형 프라이어로 yaw를 복원한다. 후보 각도 $\theta$를 스캔하면서 클러스터 점들을 $\theta$ 방향 두 축에 투영하고, "점들이 두 변에 얼마나 달라붙는가"(변까지 거리의 분산 최소화 또는 근접도 최대화) 기준으로 최적 $\theta$를 고른 뒤 그 방향의 최소 사각형을 박스로 취한다. 탐색 범위가 $[0°,90°)$면 되는 이유는 직사각형의 대칭성.

### 4.2 딥러닝 3D 검출 계보 — 희소성을 어떻게 다루나

LiDAR 포인트는 불규칙·희소하다. 계보는 "어떻게 정규 격자로 만들어 CNN/트랜스포머에 태우나"로 갈린다:

- **Voxel 계열**: 공간을 3D 복셀로 나눠 복셀별 특징 추출 후 3D 컨볼루션. 빈 복셀이 대부분이라 **희소 컨볼루션**(비어있지 않은 곳만 계산)이 실용화의 열쇠였다.
- **Pillar 계열**: z축을 아예 버리고 **기둥(pillar)** 단위로 포인트를 요약 → 의사-이미지(BEV)로 만들어 **2D CNN**만으로 처리. 3D 연산이 없어 빠르다 — 실시간 온보드의 대표 절충.
- **Point 계열**: 격자 없이 포인트를 직접 처리(PointNet류 집합 함수) — 정보 손실이 없지만 이웃 연산이 비싸다. 우리 [GraspNet 실측](grasp_sota.md)의 backbone이 이 계열(PointNet++)이다.
- **Center 계열**: 앵커 박스 대신 **BEV 히트맵으로 물체 중심**을 찾고 크기·yaw·속도를 중심점에서 회귀(anchor-free). 회전된 박스에 앵커를 정의하는 고통이 사라지고, **속도 헤드가 §5의 추적 연관 단서를 공짜로 제공**한다는 점에서 검출-추적의 경계를 허문 설계.

공통 감각: **표현의 선택(복셀/기둥/포인트/중심)이 곧 속도-정밀도 트레이드오프**다. 압축이 지우는 정보가 다운스트림 성능을 결정한다는 주제는 [world model 트랙의 DIAMOND](world-models/diamond.md)와 같은 결.

### 4.3 온보드 vs 인프라(고정 설치형)

같은 인식이라도 센서가 **차 위에 있느냐, 도로변에 고정돼 있느냐**로 문제 구조가 달라진다:

| 축 | 온보드 | 인프라 고정형 |
|---|---|---|
| ego-motion | 있음 — motion compensation·ego 보상 필수 | **없음** — 정적 배경 가정 성립 |
| 배경 처리 | 매 순간 새 장면 | **배경 모델링/차분**으로 동적 물체만 분리 가능 |
| 캘리브레이션 | 진동·온도로 드리프트, 주기 재캘리브 | 설치 시 1회 정밀 캘리브가 오래 감 |
| 가림(occlusion) | 시점 하나 — 구조적 사각 | 높은 시점·다센서 배치로 완화 가능 |
| 제약 | 전력·연산 예산 빡빡 | 상대적으로 여유, 대신 통신 지연 |

고정 설치형은 "SLAM이 필요 없는 세계"라는 점이 재미있다 — ego-pose 문제가 사라지고 추정 예산 전부가 §5의 객체 추적에 집중된다. 반대로 온보드는 [SLAM/odometry](slam.md)와 추적이 한 시스템 안에서 좌표를 공유해야 한다(§5.5).

---

## 5. 다중 객체 추적 (MOT)

### 5.1 구조 — 4단계 루프

$$
\text{검출} \longrightarrow \text{데이터 연관} \longrightarrow \text{상태 추정} \longrightarrow \text{트랙 관리} \longrightarrow (\text{다음 프레임})
$$

- **검출**: §4의 출력 — 프레임마다 박스 집합(트랙 정보 없음).
- **연관(association)**: "이번 프레임의 검출 $j$가 기존 트랙 $i$의 그 물체인가" — MOT의 심장.
- **상태 추정**: 트랙별 §1의 필터(CV-칼만이 기본) — 연관된 검출을 측정으로 갱신, 연관 실패 시 예측만으로 관성 유지.
- **트랙 관리**: 생성·확정·소멸의 수명 주기.

**tracking-by-detection**이라 부르는 이 구조가 표준인 이유: 검출기와 추적기를 분리하면 각각 독립적으로 교체·개선할 수 있고, 추적층은 가볍게(필터+할당) 유지된다.

### 5.2 데이터 연관 — 비용의 유도

트랙 $i$와 검출 $j$ 사이 비용 행렬 $C_{ij}$를 만들고 최적 할당을 푼다.

**기하 비용 — IoU**: $C_{ij} = 1-\text{IoU}(\hat B_i, B_j)$. 박스가 크고 프레임률이 높아 겹침이 보장될 때 단순·강력.

**확률 비용 — 마할라노비스 거리.** 트랙 필터의 예측이 $\mathcal N(H\hat x^-, S)$일 때($S$는 §1.3의 잔차 공분산), 측정 $z_j$의 가우스 로그우도는

$$
-\log p(z_j) = \tfrac12\,\underbrace{(z_j-H\hat x^-)^\top S^{-1} (z_j-H\hat x^-)}_{d_M^2\ (\text{마할라노비스 제곱})} + \tfrac12\log\det(2\pi S)
$$

즉 **마할라노비스 거리 = 우도 기반 연관 비용**이다. 유클리드가 아니라 이걸 쓰는 이유가 유도에서 바로 보인다 — $S^{-1}$이 끼므로 **불확실성이 큰 방향의 편차는 관대하게, 확실한 방향의 편차는 엄하게** 잰다. 빠르게 달리는 트랙은 진행 방향으로 공분산이 길쭉하니, 그 방향으로 떨어진 검출도 같은 물체로 받아준다.

**게이팅**: $d_M^2$은 (연관이 맞다면) 자유도 = 측정 차원인 $\chi^2$ 분포를 따르므로, $\chi^2$ 분위수로 컷을 걸어 명백한 오연관을 후보에서 제외한다.

**할당**: 비용 행렬 위의 최적 1:1 매칭은 **헝가리안 알고리즘**(이분 매칭의 다항시간 최적해, $O(n^3)$). greedy(최소 비용부터 순차 확정)는 근사지만 빠르고, 검출 신뢰도로 정렬해 단계적으로 매칭하는 변형(높은 신뢰 먼저, 남은 트랙에 낮은 신뢰 재도전)이 가림 상황에서 효과적이라는 것이 최근 계보의 발견이다.

### 5.3 트랙 관리 — 수명 주기

- **생성(birth)**: 미연관 검출 → 잠정(tentative) 트랙. **M/N 규칙**(최근 $N$프레임 중 $M$번 연관되면 확정)으로 위양성 검출이 트랙이 되는 걸 막는다.
- **소멸(death)**: 연관 실패가 $T_{\text{miss}}$프레임 연속되면 삭제. 그동안은 예측만으로 coast — 짧은 가림을 관통하는 장치.
- **ID 유지**: 트랙이 죽었다 새로 생기면 ID switch — MOT 품질의 핵심 실패 모드(§7 지표).

### 5.4 대표 계보 한 줄씩

| 계보 | 한 줄 요지 |
|---|---|
| SORT | CV-칼만 + IoU 비용 + 헝가리안 — "이 정도로 된다"를 보인 최소 구성 |
| DeepSORT | + 외형 임베딩(재식별 특징)과 마할라노비스의 결합 비용 — 가림 후 재연관 강화 |
| 3D MOT 기본형 | SORT의 3D판 — 3D 박스 상태(위치·크기·yaw)로 칼만, 3D IoU 연관 |
| Center 계열 추적 | 검출기의 **속도 헤드**로 이전 프레임 중심에 역투영해 연관 — 필터·헝가리안 없이 greedy 근접 매칭 |
| 저신뢰 재활용 | 높은 신뢰 검출 먼저 매칭 → 남은 트랙을 낮은 신뢰 검출과 재매칭 — 가림 중 "흐릿한 검출"을 버리지 않는다 |

### 5.5 3D·차량 특수성

- **yaw wrap**: 각도는 $\pi$ 근처에서 튄다 — 잔차를 $(-\pi,\pi]$로 정규화해 필터에 넣어야 한다(안 하면 한 바퀴 도는 유령 회전이 속도로 잡힘).
- **ego-motion 보상**: 온보드 추적은 자차가 움직이므로, 트랙을 **월드(또는 로컬 맵) 좌표**에서 유지하고 검출을 ego-pose로 변환해 넣는다 — [SLAM/odometry](slam.md)의 출력이 추적의 입력이 되는 접점. ego-pose 오차는 그대로 모든 트랙의 유령 속도가 된다(오차 전파).
- **관측 안 되는 상태**: 검출은 위치·크기·yaw만 준다 — 속도·yaw-rate는 §1.4처럼 필터가 시계열에서 추출한다. 추적의 출력(위치+속도+공분산)이 예측(prediction)·계획(§6)의 입력이 된다.

---

## 6. 모션 플래닝

### 6.1 계층 구조

$$
\text{경로(route)} \longrightarrow \text{거동(behavior)} \longrightarrow \text{궤적(trajectory)} \longrightarrow \text{제어(control)}
$$

- **경로**: 도로망 그래프에서 A→B (수 km 스케일, §6.2의 그래프 탐색).
- **거동**: 차선 유지/변경, 정지, 양보 같은 이산 결정(상태기계·규칙 또는 학습).
- **궤적**: 수 초 지평의 시공간 곡선 $x(t)$ — 동역학·충돌 제약을 만족하는 연속 최적화(§6.4).
- **제어**: 궤적을 따라가는 조향·가감속(PID·MPC) — [manipulation 트랙](manipulation.md)의 "타깃 → IK/제어"와 같은 층이다.

### 6.2 그래프 탐색 — Dijkstra에서 A*, Hybrid A*까지

**Dijkstra**: 시작점부터 비용 오름차순으로 노드를 확정(우선순위 큐) — 모든 방향으로 균등하게 퍼진다. **A***는 여기에 목표까지의 추정 잔여비용 $h(n)$을 더해 탐색을 목표 쪽으로 끌어당긴다:

$$
f(n) = \underbrace{g(n)}_{\text{시작→}n\text{ 실비용}} + \underbrace{h(n)}_{\text{n→목표 추정}}
$$

**최적성 조건 유도 스케치**: $h$가 **admissible**(실제 잔여비용을 절대 과대평가하지 않음: $h(n)\le h^*(n)$)이면, 목표가 큐에서 꺼내지는 순간 $f=g$가 실제 최적비용 이하임이 보장돼 A*는 최적해를 반환한다. 과대평가하는 $h$는 빠르지만 최적성을 잃는다(가중 A*의 의도적 트레이드). $h$가 **consistent**($h(n)\le c(n,n')+h(n')$, 삼각부등식)이면 노드 재방문이 없어진다. 유클리드 거리가 도로 그래프의 admissible한 $h$의 대표.

**Hybrid A***: 격자 A*의 문제는 차가 **비홀로노믹**(제자리 회전 불가, 최소 회전반경)이라는 걸 무시한다는 것. Hybrid A*는 셀마다 이산 상태가 아니라 **연속 상태 $(x,y,\theta)$**를 저장하고, 이웃 확장을 상하좌우가 아니라 **차량 운동 모델로 적분한 짧은 호**(arc)로 한다 — 나오는 경로가 그대로 주행 가능(kinematically feasible)하다. 주차·좁은 공간 기동의 표준.

### 6.3 샘플링 기반 — RRT와 RRT*

고차원·복잡 제약 공간에선 격자가 폭발한다. **RRT**는 공간을 무작위로 더듬는다:

```
반복: q_rand ← 자유공간 무작위 샘플 (일정 확률로 목표를 샘플 = goal bias)
      q_near ← 트리에서 q_rand에 가장 가까운 노드
      q_new  ← q_near에서 q_rand 방향으로 스텝 δ만큼 전진 (충돌 검사)
      트리에 q_new 추가; 목표 근방 도달 시 종료
```

- 성질: **확률적 완전성**(해가 있으면 샘플 수 → ∞에서 찾을 확률 → 1). 단, 경로 품질 보장은 없다 — 최초 해는 대개 구불구불하다.
- **RRT***가 이를 고친다. $q_{new}$를 넣을 때 ① 근방 반경 내 노드들 중 **최소 비용 부모를 다시 고르고**(choose-parent), ② 근방 노드들의 부모를 $q_{new}$ 경유로 바꾸는 게 이득이면 **재배선**(rewire)한다. 이 두 국소 수술 덕에 샘플이 쌓일수록 트리 비용이 단조 개선 — **점근 최적성**을 얻는다(근방 반경을 $\gamma(\log n/n)^{1/d}$로 줄여가는 조건 하).

### 6.4 궤적 최적화 — Frenet 분해와 최소 jerk

**Frenet frame**: 도로 중심선을 따라가는 호길이 $s$(종방향)와 중심선에서의 횡오프셋 $d$(횡방향)로 좌표를 바꾸면, "차선을 따라 달리며 옆으로 얼마나"라는 주행의 구조가 좌표에 내장된다 — 곡선 도로 문제가 두 개의 1D 문제 $s(t), d(t)$로 분해된다.

**최소 jerk 다항식**: 승차감의 대리 지표인 jerk($\dddot x$) 제곱적분을 최소화하는 궤적은 **5차 다항식**이다 — 경계조건(시작·끝의 위치·속도·가속도 6개)이 계수 6개를 정확히 결정한다:

$$
x(t)=\sum_{i=0}^{5} a_i t^i,\qquad
\big[x,\dot x,\ddot x\big]_{t=0}=\text{현재 상태},\quad
\big[x,\dot x,\ddot x\big]_{t=T}=\text{목표 상태}.
$$

실무 레시피: 목표 상태($d$의 후보 오프셋, $s$의 후보 속도)와 지평 $T$를 격자로 뿌려 후보 궤적 다발을 만들고, 각 후보를 비용(jerk + 시간 + 횡편차 + 충돌·가속도 제약 위반)으로 채점해 최소를 고른다 — **샘플링과 최적화의 절충**이 이 층의 표준 감각이다.

**MPC 관점**: 더 원리적으로는 이산화한 차량 모델 $x_{k+1}=Ax_k+Bu_k$에 대해 지평 $N$의 제약付き 이차계획(QP)을 매 스텝 풀고 첫 입력만 실행한다(receding horizon):

$$
\min_{u_{0:N-1}}\ \sum_{k=0}^{N-1}\Big(\|x_k-x_k^{\text{ref}}\|_Q^2+\|u_k\|_R^2\Big)+\|x_N-x_N^{\text{ref}}\|_{Q_f}^2
\quad\text{s.t.}\quad x_{k+1}=Ax_k+Bu_k,\ \ x\in\mathcal X_{\text{free}},\ u\in\mathcal U.
$$

§1의 칼만 필터와 묘하게 대칭이다 — 추정은 과거 측정의 가중 최소자승, MPC는 미래 오차의 가중 최소자승. 같은 이차 최적화가 시간축 방향만 바꿔 재등장한다. ([TD-MPC 리뷰](world-models/tdmpc.md)의 "지평 안은 모델, 지평 밖은 가치"가 이 틀의 학습판.)

**차량 모델 — kinematic bicycle**: 저속·비드리프트 영역의 표준 근사. 축거 $L$, 조향각 $\delta$일 때

$$
\dot x = v\cos\theta,\qquad \dot y = v\sin\theta,\qquad \dot\theta = \frac{v}{L}\tan\delta
$$

— 뒷바퀴 기준, 앞바퀴가 만드는 회전반경 $R = L/\tan\delta$에서 곧장 나온다. Hybrid A*의 확장 프리미티브, MPC의 $A,B$ 선형화 지점이 모두 이 모델이다.

### 6.5 예측과의 경계

계획의 입력은 정적 지도가 아니라 §5의 출력 — **각 객체의 상태와 불확실성, 그리고 예측된 미래 궤적**이다. 예측이 다봉(교차로에서 직진일까 좌회전일까)이라는 점이 이 경계의 본질적 난제고, 최근 계보가 학습 기반 다중 가설 예측으로 옮겨간 이유다. 불확실성을 계획 비용에 어떻게 반영하나(공분산 팽창 마진, 확률적 충돌 제약)가 이 경계의 설계 변수.

---

## 7. 평가 지표

| 층 | 지표 | 정의 요지 |
|---|---|---|
| 검출 | IoU, AP | 정합 기준(2D/BEV/3D IoU 또는 중심거리)으로 TP 판정 → precision-recall 곡선 아래 면적 |
| 추적 | **MOTA** | $1-\dfrac{\text{FN}+\text{FP}+\text{IDSW}}{\text{GT}}$ — 누락·오검·ID 스위치를 한 번에 벌점 |
| 추적 | MOTP | 매칭된 쌍의 평균 정합 오차 — 정확도(위치 품질)만 분리 |
| 추적 | IDF1 / ID 지표 | ID 보존 관점의 F1 — "같은 물체를 끝까지 같은 ID로 따라갔나" |
| 계획 | 충돌률·제약 위반·jerk·진행률 | 안전(하드)과 승차감·효율(소프트)의 분리 채점 |

읽는 감각: MOTA는 검출 품질에 강하게 지배되고(FN·FP가 보통 IDSW보다 수십 배 많다), ID 계열 지표가 연관 품질을 따로 비춘다 — 지표 하나로 시스템을 말하지 않는 것이 이 동네의 상식이다.

---

## 8. 이 트랙과의 연결 + 다음 실습 후보

**이미 실측으로 갖고 있는 연결점** (각 페이지에 기록):

- **시간 정렬을 직접 풂**: cam-IMU 각속도 프로파일 정합 — 오프셋 2.1ms, extrinsic 0.26° ([perception](perception.md)) — §2.1의 동기화 층.
- **공간 정렬을 직접 풂**: hand-eye **AX=XB** 자체 구현(위치 4.24mm/회전 0.096°) — §2.1의 extrinsic 층. 회전축 다양성 없으면 병진 관측 불가라는 랭크 구조까지.
- **센서 추가의 가치를 A/B로 실측**: mono 7.8mm → stereo 0.4mm / RGB-D 0.9mm ([perception](perception.md)) — §2.3 상보성의 소규모 실증.
- **같은 수학의 반복**: ICP의 소각 선형화-정규방정식([slam_math §2](slam_math.md)) ↔ EKF의 야코비안 선형화(§1.5), pose-graph 정보 행렬([slam_math §6](slam_math.md)) ↔ 융합의 정밀도 덧셈(§2.2), RANSAC([매칭](reviews/matching.md)) ↔ 지면 제거(§4.1) — 도구 상자는 하나다.
- **PointNet++ 계열을 실제로 돌림**: [GraspNet 6모델 A/B](grasp_sota.md) — §4.2 point 계열의 몸.

**다음 실습 후보** (이 장을 hands-on으로 승격시키는 최단 경로):

1. **미니 MOT**: 공개 주행 데이터셋 시퀀스 하나로 CV-칼만 + 마할라노비스 게이팅 + 헝가리안 트래커를 밑바닥 구현 — MOTA/IDF1 채점까지. §1·§5 전부가 코드 몇백 줄에 담긴다.
2. **고전 LiDAR 파이프라인**: RANSAC 지면 제거 → DBSCAN → L-shape 피팅을 한 스캔에 구현, GT 박스와 IoU 채점 — §4.1의 실측화.
3. **Frenet 궤적 후보 생성기**: 5차 다항식 다발 + 비용 채점을 시각화 — §6.4를 눈으로 확인.
